import styled from 'styled-components'

export const Wrapper = styled.div`
  height: 8px;
  border-radius: 2px;
  background-color: rgba(231, 52, 122, 0.2);
  overflow: hidden;

  > div {
    height: 8px;
    border-radius: 2px;
  }
`
